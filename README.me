批量下载12306网站的图片验证码到本地。
采用代理IP池和用户代理池，尽量减少频繁访问带来的返回刷新频率太快而不是验证码图片的问题。
因为该网站返回的图片验证码有重复的问题，所以采用了hash函数去重。
在config.py文件中可以配置：
    存储位置：需一个绝对路径，默认为该文件夹下的captcha文件夹中；
    线程数：采用多线程下载，消息列队，用以加快运行速度。默认的线程数为cpu核数；
    下载任务数：指的是下载图片数，但是有重复问题，去重后的实际图片数要小于该数。